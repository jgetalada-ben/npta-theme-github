@font-face{
  font-family: "Anton";
  src:
    url({{ 'Anton-Regular.woff2' | asset_url }}) format("woff2"),
    url({{ 'Anton-Regular.woff'  | asset_url }}) format("woff");
  font-weight: 400;
  font-style: normal;
  font-display: swap;
}
